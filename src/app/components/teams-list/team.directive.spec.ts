import { TeamDirective } from './team.directive';

describe('TeamDirective', () => {
  it('should create an instance', () => {
    const directive = new TeamDirective();
    expect(directive).toBeTruthy();
  });
});
